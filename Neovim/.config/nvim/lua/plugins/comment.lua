return { -- TODO: Find a different (preferably inbuilt) solution for this
  "numToStr/Comment.nvim",
  enabled = true,
  opts = {},
  keys = {
    { "gc", mode = { "n", "v" } },
    { "gb", mode = { "n", "v" } },
  },
}
